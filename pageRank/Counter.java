package cs3390;

public enum Counter {
	PageCounter  /* This is the method of creating a gounter 
					
				    to use in mapper/reducer do, 
				    context.getCounter(Counter.PageCounter).increment(1);
				 */
}
